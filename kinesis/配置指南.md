# Kinesis 配置指南

## 🎯 您的文件已经下载好了！

✅ **SMPL_NEUTRAL.pkl** 在正确位置  
✅ **KIT.tar.bz2** 在正确位置

---

## 📋 现在该做的3件事

### 第1步：验证SMPL文件（10秒）

**方法A - 命令行：**
```bash
python D:\兼职\18\Kinesis\config.py
```

**方法B - Python测试：**
打开Python，运行：
```python
import joblib
import os

# 检查SMPL
path = r"D:\兼职\18\Kinesis\data\smpl\SMPL_NEUTRAL.pkl"
if os.path.exists(path):
    data = joblib.load(path)
    print("✓ SMPL模型OK")
    print(f"  包含{len(data)}个字段")
else:
    print("✗ SMPL模型不存在")
```

---

### 第2步：解压KIT数据集（2-5分钟）

**如果KIT目录不存在（data\KIT_Data\KIT\），需要解压：**

**运行命令：**
```bash
python D:\兼职\18\Kinesis\config.py --extract
```

**或者在Python中运行：**
```python
import tarfile
import os

# 解压
tar_path = r"D:\兼职\18\Kinesis\data\KIT_Data\KIT.tar.bz2"
extract_to = r"D:\兼职\18\Kinesis\data\KIT_Data"

tar = tarfile.open(tar_path, 'r:bz2')
tar.extractall(extract_to)
tar.close()

print("✓ 解压完成")
```

---

### 第3步：转换KIT数据（10-15分钟）

**解压完成后，运行：**
```bash
python D:\兼职\18\Kinesis\src\utils\convert_kit.py --path "D:\兼职\18\Kinesis\data\KIT_Data\KIT"
```

**或者使用相对路径：**
```bash
cd D:\兼职\18\Kinesis
python src\utils\convert_kit.py --path "data\KIT_Data\KIT"
```

**等待约10-15分钟**

---

## ✅ 完成后验证

运行配置检查：
```bash
python D:\兼职\18\Kinesis\config.py
```

**预期输出：**
```
[OK] SMPL_NEUTRAL.pkl: XX.XX MB
[OK] KIT.tar.bz2: XXXX.XX MB
[OK] KIT directory: XXXX NPZ files
[OK] kit_train_motion_dict.pkl: XX.XX MB
[OK] kit_test_motion_dict.pkl: XX.XX MB
[SUCCESS] All files are ready!
```

---

## 🚀 开始运行测试

### 测试1：目标到达（最简单）

```bash
cd D:\兼职\18\Kinesis
python src\run.py exp_name=kinesis-target-goal-reach run=eval_run learning=pointgoal epoch=-1 run.headless=True
```

### 测试2：运动模仿

```bash
cd D:\兼职\18\Kinesis
python src\run.py exp_name=kinesis-moe-imitation epoch=-1 run=eval_run run.headless=True run.motion_file=data\kit_test_motion_dict.pkl run.initial_pose_file=data\initial_pose\initial_pose_test.pkl env.termination_distance=0.5
```

---

## 💡 快速检查清单

### 运行前确认：

- [ ] SMPL_NEUTRAL.pkl 存在 data\smpl\
- [ ] KIT 数据集已解压到 data\KIT_Data\KIT\
- [ ] 运行了 convert_kit.py 转换
- [ ] kit_train_motion_dict.pkl 存在 data\
- [ ] kit_test_motion_dict.pkl 存在 data\

---

## ❓ 如果遇到问题

### 问题1：Python找不到
**A:** 确认Python已安装：
```bash
python --version
```

### 问题2：找不到文件
**A:** 使用绝对路径，如：
```
D:\兼职\18\Kinesis\data\smpl\SMPL_NEUTRAL.pkl
```

### 问题3：转换失败
**A:** 检查：
- KIT目录是否有NPZ文件
- 是否有读取权限
- 查看错误信息

### 问题4：运行测试失败
**A:** 先运行：
```bash
python config.py
```
检查所有文件是否就绪

---

## 📄 我为您创建的工具

| 文件 | 用途 |
|------|------|
| **config.py** | 配置检查和解压工具 |
| **check_setup.py** | 详细文件检查 |
| **check_files.bat** | Windows批处理检查 |
| **extract_kit.bat** | Windows解压工具 |

---

## 🎯 现在立即开始

**最快的方法：**

1. **打开命令行或PowerShell**

2. **运行：**
```bash
python D:\兼职\18\Kinesis\config.py
```

3. **根据输出继续：**
   - 如果需要解压：运行 `config.py --extract`
   - 如果需要转换：运行 `convert_kit.py`
   - 如果都就绪：运行测试

---

**开始运行吧！** 🚀
